export const defaultFlags = {
	cache: 0,
	cors: false,
	dirs: true,
	gzip: true,
	http2: false,
	logs: false,
	open: false,
	port: 8080,
	host: "localhost",
};

export const defaultParameters = {
	"0": `${process.cwd()}/`,
};
